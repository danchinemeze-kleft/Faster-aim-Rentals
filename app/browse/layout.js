export const metadata = {
  title: 'Browse Listings – Mr. Rent',
  description: 'Browse verified rental properties across Nigeria.',
}

export default function BrowseLayout({ children }) {
  return children
}
